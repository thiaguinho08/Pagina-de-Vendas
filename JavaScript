// Lista de produtos
const produtos = [
    { nome: "Camiseta", preço: 29.90, descrição: "Camiseta de algodão" },
    { nome: "Tênis", preço: 129.90, descrição: "Tênis confortável para corridas" },
    { nome: "Laptop", preço: 2999.90, descrição: "Laptop com processador Intel i7" },
    { nome: "Caderno", preço: 15.90, descrição: "Caderno espiral para anotações" },
    { nome: "Caneta", preço: 2.50, descrição: "Caneta azul esferográfica" }
  ];
  
  // Função para buscar produtos pelo nome
  function buscarProduto(nome) {
    return produtos.filter(produto => produto.nome.toLowerCase().includes(nome.toLowerCase()));
  }
  
  // Exemplo de uso
  const nomeBusca = prompt("Digite o nome do produto que você deseja buscar:");
  
  const resultados = buscarProduto(nomeBusca);
  
  if (resultados.length > 0) {
    console.log("Resultados encontrados:");
    resultados.forEach(produto => {
      console.log(`Nome: ${produto.nome}`);
      console.log(`Preço: R$${produto.preço.toFixed(2)}`);
      console.log(`Descrição: ${produto.descrição}`);
      console.log('---');
    });
  } else {
    console.log("Nenhum produto encontrado.");
  }
  
